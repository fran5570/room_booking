# Este archivo fue creado inicialmente para definir el modelo de usuario (User),
# pero no se utilizó porque el proyecto trabaja directamente con SQLite sin clases modelo.
# Se mantiene por organización y futuras extensiones.
